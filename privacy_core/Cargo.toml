# privacy_core/Cargo.toml
[package]
name = "privacy_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "privacy_core"
crate-type = ["lib"]

[dependencies]
spin = "0.9"
lazy_static = "1.4"

[features]
default = []
no_std = []

[package.metadata.docs.rs]
targets = ["x86_64-unknown-none"]
